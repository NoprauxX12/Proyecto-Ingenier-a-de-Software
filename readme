instructions:
1. change files's name .env-change to .env.
2. change data in the variables:
DB_HOST=localhost
DB_USER=myuser
DB_PASS=mypassword
DATABASE="el_que_sabe"
PORT=port_to_use

to your database own data in the .env file.